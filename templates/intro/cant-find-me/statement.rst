Find a file in ``__TPL_DB_PATH`` whose name contains the word ``__TPL_WORD``. That's where the flag is.

Connect using SSH to **__TPL_CONNECT_TO**. The account password is **__TPL_PASSWORD**.

__TPL_PLATFORM_MESSAGE
